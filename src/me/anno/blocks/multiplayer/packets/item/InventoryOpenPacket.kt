package me.anno.blocks.multiplayer.packets.item

/*class InventoryOpenPacket(
    var inventory: Inventory? = null
) : Packet(INVENTORY_OPEN, ExclusiveSender.Client) {

    override fun onSend(output: DataOutputStream) {
        // output.writeVec(location)
    }

    override fun onReceive(input: DataInputStream) {
        // input.readVec(location)
    }

    override fun onClient(client: Client) {
        // todo open UI
        // todo there the user can change the order of items
        // todo and he can craft...
    }

    companion object {
        val MAX_DISTANCE = 4.5f
    }

}*/